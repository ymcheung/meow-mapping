---
const isHomepage = Astro.url.pathname === '/';
---

<ul class:list={isHomepage ? 'homepage' : ''}>
  <slot />
</ul>

<style>
  ul {
    margin-block-start: var(--size-9);
    padding-inline-start: 0;
  }

  ul.homepage {
    margin-block-start: var(--size-4);
  }
</style>
